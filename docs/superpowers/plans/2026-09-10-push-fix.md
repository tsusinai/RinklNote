# 推送链路修复 — 实施计划

> **状态（2026-09-10 18:40 更新）**：Task 1–7 代码已全部写入（含测试用例），**Task 8 全量验证未执行**
> —— 本会话无法起任何外部进程（Bash 用户名撇号 bug + PowerShell 命令校验层拦截全部外部 exe），
> 编译/测试须由用户跑（命令见 Global Constraints 末尾）。所有改动视为「已写好、未验证」。

> 规格：`docs/superpowers/specs/2026-09-10-push-fix-design.md`（已确认）
> 范围：QQ 推送 + App 通知 + AI 页内注入验证
>
> ⚠️ **环境前置（必须先解决）**：本会话**无法执行任何外部程序** —— Bash 工具因用户名撇号被吞而 exit 125；
> PowerShell 工具的命令校验层拦截一切外部 exe（`&` 静默不执行、管道报错、`New-Object …ProcessStartInfo` 与
> `Start-Process` 均不生效）。**因此下列每条"验证"步骤我暂时无法自己跑。**
> 恢复顺序：① 完全退出 WorkBuddy（含托盘）重启 → 仍不行则注销 Windows 再登录 → Bash 恢复后我跑全量测试。

## Global Constraints

- 时区一律 `Asia/Shanghai`（服务端 `InsightService.SHANGHAI`；App `bookkeepingZone()`）。
- 版本号只走 `gradle/libs.versions.toml`；不动 Maven 坐标。
- `ai_disabled` 语义不变：**总闸**，关掉全部主动推送（含日报）。新增的 `daily_report_enabled` 是日报**子开关**。
- 提交按路径显式 `git add`，**不用 `add -A`**（避免 `server/build`、`app/build` 产物入库）。
- 测试命令（有 bash）：
  ```bash
  export GRADLE_USER_HOME=D:/gradle && ./gradlew :app:testDebugUnitTest :server:test
  ```
- 测试命令（无 bash 时的手工等价物，JDK 必须用 17）：
  ```
  set GRADLE_USER_HOME=D:\gradle
  "C:\Users\a'su's\.jdks\jdk-17.0.19+10\bin\java.exe" -classpath "D:\Codes\RinklNote\gradle\wrapper\gradle-wrapper.jar" org.gradle.wrapper.GradleWrapperMain :server:test
  ```

---

## Task 1：服务端 `dailyReport` 内容补全（TDD）

**Files**
- Modify: `server/src/main/kotlin/com/example/rinklnote/server/services/BillService.kt`
- Modify: `server/src/main/kotlin/com/example/rinklnote/server/services/insight/InsightService.kt`
- Test: `server/src/test/kotlin/com/example/rinklnote/server/services/insight/InsightServiceTest.kt`

**Interfaces**
- Produces: `BillService.MonthStats(topExpenseCategories, topIncomeCategories, billCount)`；`InsightService.dailyReport()` 返回含真实金额/分类/笔数的 `summary`。

- [ ] **Step 1（红）** 在 `InsightServiceTest` 加用例（沿用本文件既有的 `insertBill` / `startOfDay` helper；收入若缺 helper 需新增一个 `insertIncome`）：

```kotlin
@Test
fun `dailyReport summary contains real amounts categories and bill count`() {
    val dayStart = startOfDay(2026, 8, 31)
    insertBill(1L, 28.0, "三餐", dayStart)
    insertBill(1L, 18.5, "交通", dayStart)
    val r = runBlocking { insight.dailyReport(1L, dayStart, dayStart + 86_400_000L) }
    assertTrue("应含支出合计 46.50", r.summary.contains("46.50"))
    assertTrue("应含分类 三餐", r.summary.contains("三餐"))
    assertTrue("应含分类 交通", r.summary.contains("交通"))
    assertEquals(2, r.billCount)
}
```

- [ ] **Step 2** 跑 `:server:test --tests "…insight.InsightServiceTest"` → **预期 FAIL**（当前 `summary` 是 `"支出: ￥"` 空壳，`billCount=0`）。

- [ ] **Step 3** 扩展 `MonthStats`（加默认值，不破坏既有调用点 `monthlySummary` / `naturalQuery`）：

```kotlin
data class MonthStats(
    val totalExpense: Double,
    val totalIncome: Double,
    val topExpenseCategories: List<Pair<String, Double>>,
    val topIncomeCategories: List<Pair<String, Double>> = emptyList(),
    val billCount: Long = 0
)
```

- [ ] **Step 4** 在 `monthlyStats` 的 `transaction { }` 内补 `COUNT(*)` 与**收入侧**分类分组（照抄现有支出侧写法，`billType` 换成 `INCOME`）：

```kotlin
val billCount = BillsTable.select(BillsTable.id.count())
    .where { (BillsTable.userId eq userId) and (BillsTable.date greaterEq monthStart) and (BillsTable.date less nextMonthStart) }
    .first()[BillsTable.id.count()]
val topIncomeCategories = /* 照 topExpenseCategories 的 GROUP BY，条件 billType == "INCOME"，取 TOP3 */
```

- [ ] **Step 5** 修 `InsightService.dailyReport` 的 `buildString` 与返回体（**这是 R2 的根治点**）：

```kotlin
val summary = buildString {
    appendLine("✅ 今日账单总结")
    appendLine("支出: ￥${"%.2f".format(stats.totalExpense)}")
    if (stats.totalIncome > 0) appendLine("收入: ￥${"%.2f".format(stats.totalIncome)}")
    appendLine("共 ${stats.billCount} 笔")
    if (stats.topExpenseCategories.isNotEmpty()) {
        appendLine("支出分类:")
        stats.topExpenseCategories.take(3).forEach {
            appendLine("  - ${it.first}: ￥${"%.2f".format(it.second)}")
        }
    }
}
return DailyReportResponse(
    date = today,
    totalExpense = stats.totalExpense,
    totalIncome = stats.totalIncome,
    expenseCategories = stats.topExpenseCategories.map { CategoryAmount(it.first, it.second) },
    incomeCategories = stats.topIncomeCategories.map { CategoryAmount(it.first, it.second) },
    billCount = stats.billCount.toInt(),
    summary = summary.toString()
)
```

- [ ] **Step 6（绿）** 跑 `:server:test --tests "…InsightServiceTest"` → PASS。
- [ ] **Step 7** 跑 `:server:test` **全量回归**（`MonthStats` 改了签名）→ 全绿。
- [ ] **Step 8 Commit**
  ```bash
  git add server/src/main/kotlin/com/example/rinklnote/server/services/BillService.kt \
          server/src/main/kotlin/com/example/rinklnote/server/services/insight/InsightService.kt \
          server/src/test/kotlin/com/example/rinklnote/server/services/insight/InsightServiceTest.kt
  git commit -m "fix(server): 日报 summary 补全金额/分类/笔数插值, MonthStats 加收入分类与笔数"
  ```

---

## Task 2：`PushScheduler` 扁平化 + `pushIfNeeded`（TDD）

**Files**
- Modify: `server/src/main/kotlin/com/example/rinklnote/server/services/PushScheduler.kt`
- Test: `server/src/test/kotlin/com/example/rinklnote/server/services/PushSchedulerTest.kt`

**Interfaces**
- Produces: `private suspend fun pushIfNeeded(userId, openid, type, dayKey, provider)`。

- [ ] **Step 1（红）** 加用例：

```kotlin
@Test
fun `daily report is pushed even when habit provider returns null`() {
    val sent = mutableListOf<String>()
    val sched = PushScheduler(
        userService, { _, c, _ -> sent.add(c); true },
        monthlyProvider = { _, _ -> null },
        anomalyProvider = { _ -> null },
        habitProvider = { _ -> null },              // ← 常态
        dailyReportProvider = { _ -> "✅ 今日账单总结" },
        clock = { LocalDateTime.of(2026, 8, 31, 10, 0) },
        log = log
    )
    sched.tick()
    assertEquals(1, sent.size)                       // 当前实现下为 0
}
```

- [ ] **Step 2** 跑 `:server:test --tests "…PushSchedulerTest"` → **预期 FAIL**（日报被嵌在 habit 分支里）。

- [ ] **Step 3** 抽 helper 并把四类推平（**R1 的根治点**；`tick()` 的 `try{}` 内变成四行）：

```kotlin
private suspend fun pushIfNeeded(
    userId: Long, openid: String, type: String, dayKey: String,
    provider: suspend (Long) -> String?
) {
    if (alreadyPushed(userId, type, dayKey)) return
    val content = provider(userId) ?: return
    if (send(openid, content, UUID.randomUUID().toString())) {
        markPushed(userId, type, dayKey)
        log.info("$type 已推送 user=$userId")
    } else {
        log.warn("$type 发送失败 user=$userId")
    }
}
```

`tick()` 主体：

```kotlin
for (u in userService.findAllBoundQq()) {
    if (u.aiDisabled) continue
    val openid = u.qqOpenid ?: continue
    try {
        if (isLastDayOfMonth && hour >= 20)
            pushIfNeeded(u.id, openid, "MONTHLY_SUMMARY", month) { monthlyProvider(it, month) }
        pushIfNeeded(u.id, openid, "ANOMALY", dayKey, anomalyProvider)
        pushIfNeeded(u.id, openid, "HABIT", dayKey, habitProvider)
        if (hour >= 9)
            pushIfNeeded(u.id, openid, "DAILY_REPORT", dayKey, dailyReportProvider)
    } catch (e: Exception) {
        log.warn("push user=${u.id} failed: ${e.message}")
    }
}
```

- [ ] **Step 4（绿）** 跑 `:server:test --tests "…PushSchedulerTest"` → PASS；既有去重 / `ai_disabled` 用例不回归。
- [ ] **Step 5 Commit**
  ```bash
  git add server/src/main/kotlin/com/example/rinklnote/server/services/PushScheduler.kt \
          server/src/test/kotlin/com/example/rinklnote/server/services/PushSchedulerTest.kt
  git commit -m "fix(server): 日报推送从习惯分支中提出并扁平化, 抽 pushIfNeeded helper"
  ```

---

## Task 3：`users` 加日报列 + 迁移 + `UserService` 读写

**Files**
- Modify: `server/src/main/kotlin/com/example/rinklnote/server/tables/UsersTable.kt`
- Modify: `server/src/main/kotlin/com/example/rinklnote/server/services/UserService.kt`
- Modify: `server/src/main/kotlin/com/example/rinklnote/server/plugins/Database.kt`
- Test: `server/src/test/kotlin/com/example/rinklnote/server/services/UserServiceTest.kt`

- [ ] **Step 1** `UsersTable` 加三列：
  ```kotlin
  val dailyReportEnabled = bool("daily_report_enabled").default(false)
  val dailyReportHour = integer("daily_report_hour").default(9)
  val dailyReportMinute = integer("daily_report_minute").default(0)
  ```
- [ ] **Step 2** `Database.kt` 的 `runMigrations` 加（沿用既有幂等 ALTER 写法）：
  ```sql
  ALTER TABLE users ADD COLUMN IF NOT EXISTS daily_report_enabled BOOLEAN DEFAULT FALSE
  ALTER TABLE users ADD COLUMN IF NOT EXISTS daily_report_hour INT DEFAULT 9
  ALTER TABLE users ADD COLUMN IF NOT EXISTS daily_report_minute INT DEFAULT 0
  ```
- [ ] **Step 3（红）** `UserServiceTest` 加：`setDailyReport(1L,true,9,30)` → `findById(1L)` 回读 `enabled=true,hour=9,minute=30`。
- [ ] **Step 4** `UserInfo` 加三字段（带默认值），`findById/findByPhone/findByQQ/findByQqOpenid` 的映射补齐（照 `aiDisabled` 的做法），并加：
  ```kotlin
  fun setDailyReport(userId: Long, enabled: Boolean, hour: Int, minute: Int) { /* update */ }
  ```
- [ ] **Step 5（绿）** `:server:test --tests "…UserServiceTest"` → PASS；再全量 `:server:test`。
- [ ] **Step 6 Commit**（三个路径，信息：`feat(server): users 加日报开关与时间列 + 读写`）

---

## Task 4：`AuthRoutes` 日报设置端点

**Files**
- Modify: `server/src/main/kotlin/com/example/rinklnote/server/routes/AuthRoutes.kt`

- [ ] **Step 1** `MeResponse` 加 `val dailyReportEnabled: Boolean = false, val dailyReportHour: Int = 9, val dailyReportMinute: Int = 0`，`/me` 处补赋值。
- [ ] **Step 2** 在 `authenticate("auth-jwt")` 内加端点。**命名为 `daily-report-setting`**，避免与 App 已有的 `ApiService.getDailyReport()`（拉取日报内容）混淆：
  ```kotlin
  @Serializable data class DailyReportSettingRequest(val enabled: Boolean, val hour: Int, val minute: Int)

  get("/daily-report-setting") { /* userId → {enabled,hour,minute} */ }
  put("/daily-report-setting") { /* body → userService.setDailyReport(...) → 回读 */ }
  ```
- [ ] **Step 3** 验证：`GET /api/auth/daily-report-setting` 无 token → 401；有 token → `{"enabled":false,"hour":9,"minute":0}`；`PUT` 后 `GET` 一致。
- [ ] **Step 4 Commit**（`feat(server): /api/auth/daily-report-setting 读写 + me 返回日报设置`）

---

## Task 5：`PushScheduler` 日报时间改用用户设置

**Files**
- Modify: `server/src/main/kotlin/com/example/rinklnote/server/services/PushScheduler.kt`
- Modify: `server/src/main/kotlin/com/example/rinklnote/server/Application.kt`
- Test: `server/src/test/kotlin/com/example/rinklnote/server/services/PushSchedulerTest.kt`

- [ ] **Step 1（红）** 加用例：用户设 21:30，`clock=21:29` 不推、`clock=21:31` 推；且**未开启 `daily_report_enabled` 的用户永不推**。
- [ ] **Step 2** `tick()` 里日报条件从 `hour >= 9` 改为：
  ```kotlin
  val nowMinutes = now.hour * 60 + now.minute
  val dueMinutes = u.dailyReportHour * 60 + u.dailyReportMinute
  if (u.dailyReportEnabled && nowMinutes >= dueMinutes)
      pushIfNeeded(u.id, openid, "DAILY_REPORT", dayKey, dailyReportProvider)
  ```
  （用 `>=` 而非 `==`：30s tick 不可能精确命中某一分钟，语义是"到点之后当天首推"。）
- [ ] **Step 3（绿）** 跑测试 → PASS。
- [ ] **Step 4 Commit**（`feat(server): QQ 日报时间跟随用户设置(daily_report_enabled/hour/minute)`）

---

## Task 6：App DTO + ApiService

**Files**
- Modify: `app/src/main/java/com/example/rinklnote/data/network/dto/DTOs.kt`
- Modify: `app/src/main/java/com/example/rinklnote/data/network/ApiService.kt`

- [ ] **Step 1** `MeResponse` 加 `dailyReportEnabled / dailyReportHour / dailyReportMinute`（带默认值）；加
  ```kotlin
  @Serializable data class DailyReportSettingDto(val enabled: Boolean, val hour: Int, val minute: Int)
  ```
- [ ] **Step 2** `ApiService` 加（**注意路径用连字符的新端点，不要叫 getDailyReport**）：
  ```kotlin
  @GET("api/auth/daily-report-setting") suspend fun getDailyReportSetting(): DailyReportSettingDto
  @PUT("api/auth/daily-report-setting") suspend fun setDailyReportSetting(@Body req: DailyReportSettingDto): DailyReportSettingDto
  ```
- [ ] **Step 3** 编译验证：`:app:compileDebugKotlin`。

---

## Task 7：App「日报通知」设置卡片（补回缺失 UI）

**Files**
- Modify: `app/src/main/java/com/example/rinklnote/ui/screen/profile/ProfileScreen.kt`
- Modify: `app/src/main/java/com/example/rinklnote/notification/DailyReportReceiver.kt`

- [ ] **Step 1** `ProfileScreen` 在「我的」页加一张卡片，三行（**接上已存在但从未被调用的 `setDailyReport*`**）：
  1. `日报通知` `Switch` → `setDailyReportEnabled(it)` + 开时 `DailyReportReceiver.schedule(context,h,m)`、关时 `cancel(context)`
  2. `通知时间 09:00` 行 → `showTimePicker = true` → 复用已有 `TimePickerDialog` → `setDailyReportTime(h,m)` + 重新 `schedule`
  3. `QQ 日报` `Switch` → `setDailyReportQqBot(it)` + `api.setDailyReportSetting(...)`
- [ ] **Step 2（R4）** 打开开关时请求通知权限（`ActivityResultContracts.RequestPermission`，`Manifest.permission.POST_NOTIFICATIONS`，仅 API ≥ 33）；拒绝则开关回滚 + Toast 引导去系统设置。
- [ ] **Step 3（R5）** `DailyReportReceiver.schedule()` 抽出 `nextTriggerMillis(hour,minute)`；`onReceive` 末尾**无条件重新 schedule 次日**（无论本次是否发出通知）。
- [ ] **Step 4** 编译验证 `:app:compileDebugKotlin`。

---

## Task 8：全量验证 + 冒烟（**必须做，不许跳**）

- [ ] **Step 1** `./gradlew :app:testDebugUnitTest :server:test` 全绿。
- [ ] **Step 2** `./gradlew :app:assembleDebug` 通过。
- [ ] **Step 3** 冒烟 —— **R3/R4/R5 三重闭环**：开启日报 → 出现权限弹窗 → 允许 → 把时间设到 1 分钟后 → **收到通知且内容含金额与分类**。
- [ ] **Step 4** 隔天再验一次，确认 R5 的"只响一次"已修。
- [ ] **Step 5** QQ 端：设置 1 分钟后 → 服务端日志出现 `DAILY_REPORT 已推送 user=…` → QQ 收到日报。
- [ ] **Step 6** AI 页（R6 只验证不改码）：登录 + `ai_disabled=false` → 进入 AI 页应出现月结/异常/习惯气泡；`ai_disabled=true` → 三者均不出现但欢迎语仍在。

---

## 未列入（YAGNI）

- Web 设置页的日报开关（原作仅在 Web 做了 AI 开关；本次范围未含 Web）。
- App 端远程推送（FCM）、WorkManager 定时同步。
- 月结/异常/习惯的触发窗口调整。
